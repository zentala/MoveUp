{
  "name": "@moveup/relay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MoveUp cross-device relay — Cloudflare Worker + Durable Objects (E022)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260907.1",
    "@types/node": "^22.10.0",
    "typescript": "~5.8.3",
    "vite": "^8.2.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.129.0"
  }
}
